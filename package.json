{
  "name": "neutro-form",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter \"@neutro/*\" run build",
    "lint": "biome check packages vitest.config.ts",
    "format": "biome check --write packages vitest.config.ts",
    "test": "vitest run",
    "docs:dev": "vitepress dev docs",
    "docs:build": "pnpm --filter @neutro/form-core run build && cp packages/core/dist/index.global.js docs/public/form-core.js && cp packages/core/dist/index.js docs/public/form-core.esm.js && node -e \"const fs=require('fs'),f='docs/public/form-core.js';fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/\\/\\/#\\s*sourceMappingURL=.*\\n?/g,''))\" && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "jsdom": "^24.1.3",
    "typescript": "^5.0.0",
    "vitepress": "^1.3.0",
    "vitest": "^1.0.0"
  }
}
